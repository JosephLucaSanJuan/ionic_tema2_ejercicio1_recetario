import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_tema2_ejercicio1_recetario',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
